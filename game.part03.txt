t cage';promptAction=()=>{state.cageAttached=true;say('HATCHKEEPER','Cage locked. It is watching the route, not the bars.');};}}
    if(state.mission===2&&creature.active&&dist(pos,creature)<230){text='Space · deploy '+state.selectedTool;promptAction=promptAction||(()=>useTool());}
    $('#prompt').textContent=text||'';$('#prompt').classList.toggle('hidden',!text);
  }

  function interact(it){
    if(it.special==='moth'){state.mothFound=true;it.done=true;state.trust=clamp(state.trust+5);say('FIELD NOTE','A thing can fail at its job and still succeed at being alive. Smallness is not worthlessness.');setMeter('trust',state.trust);return;}
    if(it.special==='numberer'){state.numbererFound=true;state.bridgeProtected=true;it.done=true;state.stability=clamp(state.stability+4);say('PUBLIC WORKS','Valve LH-17 added to the emergency map. Nobody applauds. The Numberer returns underground.');setMeter('stability',state.stability);return;}
    if(it.special==='notice'){state.noticesRead++;it.done=true;say('PUBLIC ADDRESS',it.text,3800);return;}
    it.done=true;state.clues.add(it.fact);const box=$(`.fact[data-fact="${it.fact}"]`);box.classList.remove('locked');box.querySelector('span').textContent=it.text;state.trust=clamp(state.trust+2);setMeter('trust',state.trust);say(it.radio[0],it.radio[1]);
  }

  function useTool(){if(state.toolCooldown>0||state.mission!==2||!creature.active)return;const pos=player.inCar?car:player,d=dist(pos,creature);if(d>270){say('CONTROL','Too far. Close the distance first.',2200);return;}state.toolCooldown=1.1;particles.push({x:pos.x,y:pos.y,r:30,life:1,color:toolColor(state.selectedTool)});beep(state.selectedTool==='silence'?220:state.selectedTool==='enemy'?130:520,.18,.04);const result={praise:()=>{state.agitation+=18;state.leakage+=8;state.wrongTools++;say('APPETITE READER','It grows a second mane. Admiration feels like permission.');},money:()=>{state.agitation+=12;state.stability-=5;state.wrongTools++;say('CONTROL','It has learned the difference between prey and customer. That did not help.');},enemy:()=>{state.agitation+=28;state.trust-=7;state.leakage+=10;state.wrongTools++;say('WEATHER WARDEN','You gave it an enemy. It finally looks happy.');},silence:()=>{state.creatureCalm+=1.4;state.agitation-=17;state.captureProgress+=state.clues.size>=3?42:24;say('APPETITE READER','The spectacle is gone. Its appetite is visible now. Hold the quiet.');}}[state.selectedTool];result();if(state.captureProgress>=100&&state.agitation<48){state.captured=true;creature.active=false;state.trust+=7;state.stability+=4;for(let i=0;i<5;i++)particles.push({x:creature.x,y:creature.y,r:20+i*10,life:1-i*.08,color:'#d2a45f'});say('CONTROL','Predator contained alive. Excellent work.');}updateMeters();}
  function toolColor(t){return({praise:'#e7c676',money:'#78a276',enemy:'#c85c50',silence:'#86a8bf'})[t];}
  function drawSpecimen(){sctx.clearRect(0,0,specimenCanvas.width,specimenCanvas.height);const g=sctx.createRadialGradient(280,220,40,280,220,250);g.addColorStop(0,'#a58a5533');g.addColorStop(1,'#071015');sctx.fillStyle=g;sctx.fillRect(0,0,560,440);drawLion(sctx,255,230,0,2.3,28);sctx.strokeStyle='#d2a45f';sctx.lineWidth=3;sctx.strokeRect(60,45,440,330);for(let x=90;x<500;x+=46){sctx.beginPath();sctx.moveTo(x,45);sctx.lineTo(x,375);sctx.stroke();}}

  function chooseVerdict(type){state.verdict=type;$('#verdictModal').classList.add('hidden');const endings={release:{title:'Glassmarket learns to speak carefully.',text:'The Crown is released into public life. Corrupt officials fear it. So do frightened children, diplomats, actors and anyone not ready to confess themselves on command.',st:-12,tr:-18,leak:15,twist:'Within a week, true believers learn they can stand beside the lion without a sound. Certainty becomes the city’s most valuable disguise.'},restrict:{title:'The lion works behind a door anyone may leave.',text:'The Crown is used only in voluntary investigations. Every subject may refuse. Every growl may be appealed. The city calls the rule cowardice until the first innocent person walks away.',st:8,tr:15,leak:3,twist:'The minister denounces the Zoo. The Children’s Jury receives twelve thousand thank-you letters.'},molt:{title:'The predator returns wearing an exit.',text:'Molt surgeons add consent, a right of appeal and a brass plate naming its appetite. It becomes less efficient and far more trustworthy.',st:10,tr:12,leak:5,twist:'The creature sheds a beautiful black skin. The Ministry tries to patent the skin.'},sanctuary:{title:'The city keeps the truth it cannot safely use.',text:'The Crown remains alive inside a chamber where it cannot see. Corruption continues outside. So does privacy.',st:2,tr:5,leak:1,twist:'Every election season, someone promises to open the vault.'},destroy:{title:'The original dies. Its anatomy becomes public.',text:'The Mercy Butcher dissolves the creature into meaningless syllables. Newspapers print diagrams explaining why destruction was necessary.',st:5,tr:-4,leak:28,twist:'Twenty-four hours later, small joke-lions begin appearing in schoolyards. The cages were secure. The stories were not.'}};const e=endings[type];state.stability=clamp(state.stability+e.st);state.trust=clamp(state.trust+e.tr);state.leakage=clamp(state.leakage+e.leak);let infrastructure='';if(state.bridgeProtected){state.stability=clamp(state.stability+12);infrastructure=' Because you noticed the Grey Numberer, valve LH-17 closes before the viral storm reaches Low Harbour. The boring animal saves the city without appearing in a single headline.';}else{state.stability=clamp(state.stability-18);infrastructure=' At midnight, a viral storm activates every automated system at once. Nobody knows which pipe connects where. Low Harbour floods while the city debates the lion.';}if(state.mothFound){state.trust=clamp(state.trust+5);infrastructure+=' The compliment moth spends dawn in the evacuation ward, feeding on small acts of courage.';}$('#endingTitle').textContent=e.title;$('#endingText').textContent=e.text+infrastructure;$('#endStability').textContent=Math.round(state.stability);$('#endTrust').textContent=Math.round(state.trust);$('#endLeak').textContent=Math.round(state.leakage);const score=state.stability+state.trust-state.leakage-state.wrongTools*4+(state.mothFound?5:0)+(state.numbererFound?10:0);$('#endRank').textContent=score>145?'Senior Keeper':score>115?'Field Naturalist':score>85?'Probationary Keeper':'Public Liability';$('#endingTwist').textContent=e.twist;$('#ending').classList.remove('hidden');}

  function interactKey(){if(promptAction)promptAction();}
  function frame(t){if(!running)return;const dt=Math.min(.033,(t-last)/1000||.016);last=t;update(dt);drawWorld();drawMinimap();requestAnimationFrame(frame);}
  function start(){$('#intro').classList.add('hidden');$('#shell').classList.remove('hidden');running=true;resize();updateMeters();setMission('DISPATCH','Possible predator manifestation','Reach Open Market. Civilian inventor remains on scene.');say('CONTROL','Keeper Unit Seven, manifestation reported near Open Market. Possible predator. Civilian inventor still on scene.');requestAnimationFrame(frame);}

  window.addEventListener('keydown',e=>{keys[e.code]=true;if(e.code==='KeyQ'){state.siren=!state.siren;say('KEEPER UNIT SEVEN',state.siren?'Siren active. Predators notice spectacle.':'Siren muted.');}if(e.code==='KeyE'){e.preventDefault();interactKey();}if(e.code==='Space'){e.preventDefault();useTool();}if(/^Digit[1-4]$/.test(e.code)){const t=['praise','money','enemy','silence'][Number(e.code.slice(-1))-1];selectTool(t);}});window.addEventListener('keyup',e=>{keys[e.code]=false;});window.addEventListener('resize',resize);
  function selectTool(t){state.selectedTool=t;$$('.tool-belt button').forEach(b=>b.classList.toggle('selected',b.dataset.tool===t));}
  $$('.tool-belt button').forEach(b=>b.addEventListener('click',()=>selectTool(b.dataset.tool)));$$('.verdict-options button').forEach(b=>b.addEventListener('click',()=>chooseVerdict(b.dataset.verdict)));$('#startBtn').addEventListener('click',start);$('#soundBtn').addEventListener('click',()=>{soundOn=!soundOn;$('#soundBtn').textContent=soundOn?'♫':'♪';if(soundOn)beep(600,.08,.03);});$('#caseToggle').addEventListener('click',()=>$('#casebook').classList.toggle('hidden'));$('#touchInteract').addEventListener('click',interactKey);$('#touchTool').addEventListener('click',useTool);

  const stick=$('#stick'),nub=stick.querySelector('i');let stickId=null,origin=null;
  function stickMove(e){const touch=[...e.changedTouches].find(t=>t.identifier===stickId);if(!touch)return;const dx=touch.clientX-origin.x,dy=touch.clientY-origin.y;con